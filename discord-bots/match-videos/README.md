# Match videos bot (optional)

**You do not need this folder** if GPSL polls Discord via Supabase cron
(same as Friendlies). That is the recommended path — see
[`docs/match-videos-setup.html`](../../docs/match-videos-setup.html).

This package is only for an optional always-on `MESSAGE_CREATE` listener.
